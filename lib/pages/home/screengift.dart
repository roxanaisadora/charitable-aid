

import 'package:flutter/material.dart';

class Secreen2 extends StatelessWidget {
  const Secreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SingleChildScrollView(
        child: Center(child: const Text('prueba')),
      ),
    );
    
  }
}